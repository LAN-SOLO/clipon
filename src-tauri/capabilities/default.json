{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "clipon windows",
  "windows": ["main", "picker"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "x-apple.systempreferences:*" }]
    }
  ]
}
